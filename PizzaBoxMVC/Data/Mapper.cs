using System;
using System.Collections.Generic;
using System.Text;
using System.Linq;


namespace Data.DataModel
{
    public static class Mapper
    {
        /*
        public static DataModel.UserName Map(UserName userName) => new DataModel.UserName
        {
            Email = userName.Email,
            FirstName = userName.FirstName,
            LastName = userName.LastName,
            Phone = userName.Phone,
        };

        public static UserName Map(DataModel.UserName userName) => new UserName
        {
            Email = userName.Email,
            FirstName = userName.FirstName,
            LastName = userName.LastName,
            Phone = userName.Phone,
        };

    */



    }
}
